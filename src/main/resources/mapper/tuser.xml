<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="winning.dao.TUserDao">

    <select id="getUserByLoginName" parameterType="java.lang.String" resultType="winning.bean.TUser">
        SELECT * FROM TUSER WHERE LOGINNAME = #{loginName}
    </select>

</mapper>